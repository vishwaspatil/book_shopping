<% if line_item == @current_item %>
<tr id="current_item">
 <% else %>
<tr>
<% end %>
	
			<td>
				<%= line_item.quantity %> * <%= line_item.product.title  %>
			</td>
			<td>
				<%= number_to_currency(line_item.total_price, :unit => "$") %>
			</td>
		</tr>
	